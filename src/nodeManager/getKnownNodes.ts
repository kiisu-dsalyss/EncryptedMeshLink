/**
 * Get Known Nodes Function
 * Retrieves the current map of known nodes
 */

import { NodeInfo } from './types';

export function getKnownNodes(knownNodes: Map<number, NodeInfo>): Map<number, NodeInfo> {
  return knownNodes;
}
